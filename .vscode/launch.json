{
    "configurations": [
        {
            "type": "chrome",
            "name": "Run Script: dev",
            "request": "launch",
            "url": "http://localhost:3000",
            "cwd": "${workspaceFolder}"
        }
    ]
}